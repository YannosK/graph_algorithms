// ENQUEUE THE GREY
				// new_vertex = (vp)malloc(sizeof(struct vertex));
				// new_vertex->v = r_h[neighbor]->row;
				// head->b = new_vertex;
				// new_vertex->n = head;
				// new_vertex->b = NULL;
				// head = new_vertex;